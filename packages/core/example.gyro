function convertStringToNumber(str: &string) -> i32 {
	return (str as i32);
}

const x = 5;

const x = 7; // Invalid assignment to const x

// Pointers

let a: string = "5";
convertStringToNumber(&a)

print(a);

if (a == 5) {

} elif (a !== 5) {
	throw ArgumentException("Invalid!");
}

for (let i = 0; i < a; i++) {
	print(i);
}