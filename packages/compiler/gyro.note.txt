float: variable = 5.5;
// We can define comments using double slashes.
int: main = func(...args) {
	// And we can access methods or properties using the `[]` bracket syntax.
	int: round = if args[0] > 0 then 1 else 0;

	// We can also use the `return` keyword to return a value.
	return round;
}